<h1>My Favorite Bars</h1>
<div class="list-group">
    <% for bar in @bars %>
    <a href="/bars/<%= bar[:id] %>" class="list-group-item list-group-item-action"><%= bar[:name] %></a>
    <% end %>
</div>